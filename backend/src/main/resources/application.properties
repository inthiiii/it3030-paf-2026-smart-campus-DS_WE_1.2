# Server Configuration
server.port=${PORT:8080}

# MongoDB Configuration - reads from .env file via spring-dotenv
spring.mongodb.uri=${MONGO_URI}

# Ensure Spring Boot doesn't fail if collections don't exist yet
spring.data.mongodb.auto-index-creation=true
